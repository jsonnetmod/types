name: test

on:
  pull_request: { }
  push: { }

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: './tests'
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '^1.16'
      - run: make test
